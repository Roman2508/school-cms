{
  "collectionName": "components_shared_button_links",
  "info": {
    "displayName": "Button link",
    "icon": "bulletList"
  },
  "options": {},
  "attributes": {
    "text": {
      "type": "string",
      "required": true
    },
    "link": {
      "type": "string",
      "required": true
    },
    "size": {
      "type": "enumeration",
      "required": true,
      "default": "full",
      "enum": [
        "full",
        "auto"
      ]
    },
    "align": {
      "type": "enumeration",
      "required": true,
      "default": "left",
      "enum": [
        "left",
        "center",
        "right"
      ]
    }
  },
  "config": {}
}
